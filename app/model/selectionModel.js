import mongoose  from "mongoose";
const { Schema, model} = mongoose;
const selectionSchema = new Schema({


})
const selectionModel = model('selection', selectionSchema)
export default selectionModel;